import React from "react";
import { StyleSheet } from "react-native";
import { Text } from "react-native-elements";

const AuthForm = () => {
  return <Text>AuthForm</Text>;
};

const styles = StyleSheet.create({});

export default AuthForm;
